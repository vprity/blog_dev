<form action="{{ url('author.post_delete', {id: post.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit">
        <i class="fal fa-trash" aria-hidden="true"></i>
        <span>Remove</span>
    </button>
</form>